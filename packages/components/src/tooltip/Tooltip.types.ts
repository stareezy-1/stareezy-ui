/**
 * Tooltip.types.ts — enums and types for the Tooltip component.
 *
 * Kept separate so Tooltip.style.ts can import types without creating
 * a circular dependency with Tooltip.tsx.
 */

export type TooltipPlacement = "top" | "bottom" | "left" | "right";
