/**
 * Pagination.types.ts — enums and types for the Pagination component.
 *
 * Kept separate so Pagination.style.ts can import types without creating
 * a circular dependency with Pagination.tsx.
 */

export enum EPaginationVariant {
  Default = "default",
  Compact = "compact",
}
