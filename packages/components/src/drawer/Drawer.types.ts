/**
 * Drawer.types.ts — enums and types for the Drawer component.
 *
 * Kept separate so Drawer.style.ts can import types without creating
 * a circular dependency with Drawer.tsx.
 */

export type DrawerAnchor = "left" | "right" | "bottom";
