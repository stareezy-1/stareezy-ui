/**
 * Resizer.style.ts — style constants for the Resizer component.
 *
 * All token values accessed via .value — no hardcoded colors.
 */

import { colors } from "@stareezy-ui/tokens";

export const resizerStyles = {
  defaultHandleColor: colors.beauBlue[300].value,
} as const;
